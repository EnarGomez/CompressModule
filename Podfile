# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ArandaCompressModule' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!
  pod 'mobile-ffmpeg-full', '3.1', :modular_headers => true
  # Pods for ArandaCompressModule

  target 'ArandaCompressModuleTests' do
    # Pods for testing
  end

end
